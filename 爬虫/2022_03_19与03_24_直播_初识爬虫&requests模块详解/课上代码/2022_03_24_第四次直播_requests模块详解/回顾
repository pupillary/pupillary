

输入一个url地址.
浏览器会发送一个请求. 到服务器  -> 请求
服务器接收到请求之后. 组织一下数据
把数据返回给浏览器  -> 响应


浏览器发送请求后. 服务器只返回一个基本的html结构(没有数据)
浏览器有可能会再一次发送请求. 服务器此时返回的就是数据了
然后你浏览器会将html和数据进行整合. 整合成我们需要的样子

分成两种状态:
1. 你要得数据在页面源代码里 -> html(包含数据)
2. 你要得数据不在页面源代码里 -> html(没有数据)
